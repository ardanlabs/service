apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: netmux
  namespace: netmux

spec:
  tls:
    - hosts:
        - netmux.host.local
      secretName: host.local-tls
  rules:
    - host: netmux.host.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name:  netmux
                port:
                  number: 8080